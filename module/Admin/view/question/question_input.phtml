<?php
	if (!isset($_SESSION)) { session_start(); }

	$isSaveData = false;
	if (isset($questionData)) { $isSaveData = true; }

	$numTexts = array("①", "②", "③", "④", "⑤");
	$answerCount = 3;
?>

<?php echo $this->partial("breadcrumb", ["datas" => $breadcrumbData]); ?>

<h1 class="title"><?php echo $title; ?></h1>

<form id="form" method="post" action="./confirm">
	<?php if ($isSaveData || (isset($mode) && ($mode == "edit"))): ?>
		<input name="idx" value="<?php echo $questionData["idx"]; ?>" type="hidden"/>
	<?php endif; ?>

	<div class="emegra card">
		<div class="card-header">設定</div>

		<div class="card-body">
			<style type="text/css">
				dl.emegra.setting { grid-template-columns: 140px 1fr; }
			</style>
			<dl class="emegra setting">
				<dt class="essential">大分類</dt>
				<dd>
					<select class="form-select" name="class1st" style="width: 150px;">
						<option value="" hidden>選択</option>
						<?php foreach($class1stDatas as $idx => $data): ?>
							<option value="<?php echo $idx; ?>"<?php echo ($isSaveData && $questionData["class1st"] == $idx) ? " selected" : ""; ?>><?php echo $data; ?></option>
						<?php endforeach; ?>
					</select>
				</dd>

				<dt class="essential">小分類</dt>
				<dd>
					<select class="form-select" name="class2nd" style="width: 150px;">
						<option value="" hidden>選択</option>
						<?php foreach($class2ndDatas as $idx => $data): ?>
							<option value="<?php echo $idx; ?>"<?php echo ($isSaveData && $questionData["class2nd"] == $idx) ? " selected" : ""; ?>><?php echo $data; ?></option>
						<?php endforeach; ?>
					</select>
				</dd>
		
				<dt class="essential">レベル</dt>
				<dd>
					<select class="form-select" name="level" style="width: 100px;">
						<option value="" hidden>選択</option>
						<?php foreach($levelDatas as $idx => $data): ?>
							<option value="<?php echo $idx; ?>"<?php echo ($isSaveData && $questionData["level"] == $idx) ? " selected" : ""; ?>><?php echo $data; ?></option>
						<?php endforeach; ?>
					</select>
				</dd>

				<dt class="essential">問題タイプ</dt>
				<dd>
					<select class="form-select" name="type" id="type" onchange="ChangeType(this.value)" style="width: 150px;">
						<option value="" hidden>選択</option>
						<?php foreach($typeDatas as $idx => $data): ?>
							<option value="<?php echo $idx; ?>"<?php echo ($isSaveData && $questionData["type"] == $idx) ? " selected" : ""; ?>><?php echo $data; ?></option>
						<?php endforeach; ?>
					</select>
				</dd>
		
				<dt class="essential">タイトル</dt>
				<dd><input class="form-control" name="title" type="text" value="<?php echo ($isSaveData) ? $questionData["title"] : ""; ?>"/></dd>
			</dl>
		</div>
	</div>

	<div class="emegra card" style="margin-top: 32px;">
		<div class="card-header">問題</div>

		<div class="card-body">
			<style type="text/css">
				dl.emegra.question { grid-template-columns: 140px 1fr; }
			</style>
			<dl class="emegra question">
				<dt class="essential">問題</dt>
				<dd><input class="form-control" name="question" type="text" value="<?php echo ($isSaveData) ? $questionData["question"] : ""; ?>"/></dd>

				<dt class="essential">答え</dt>
				<div id="answers" name="answers" style="display: grid; grid-template-columns: 140px 1fr; grid-gap: 24px 8px;">
					<?php if ($isSaveData): ?>
						<?php foreach(explode(",", $questionData["answers"]) as $index => $answer): ?>
							<dt class="essential"><?= $numTexts[$index]; ?></dt>
							<dd><input class="form-control" name="answer<?= $index + 1; ?>" type="text" value="<?php echo $answer; ?>"/></dd>
						<?php endforeach; ?>
					<?php else: ?>
						<?php for($i = 0; $i < 3; $i++): ?>
							<dt class="essential"><?= $numTexts[$i]; ?></dt>
							<dd><input class="form-control" name="answer<?= $i; ?>" type="text"/></dd>
						<?php endfor; ?>
					<?php endif; ?>
				</div>

				<dt class="essential">正解番号</dt>
				<dd>
					<select class="form-select" name="correct" id="correct" style="width: 70px;">
						<option value="" hidden>選択</option>
						<?php if ($isSaveData): ?>
							<?php for ($i = 1; $i <= $questionData["type"] + 3; $i++): ?>
								<option value="<?= $i; ?>"<?php echo ($isSaveData && $questionData["point"] == $i) ? " selected" : ""; ?>><?= $numTexts[$i - 1]; ?></option>
							<?php endfor; ?>
						<?php else: ?>
							<?php for ($i = 1; $i <= 3; $i++): ?>
								<option value="<?= $i; ?>"><?= $numTexts[$i - 1]; ?></option>
							<?php endfor; ?>
						<?php endif; ?>
					</select>
				</dd>

				<dt class="essential">点数</dt>
				<dd>
					<select class="form-select" name="point" style="width: 70px;">
						<option value="" hidden>選択</option>
						<?php for($i = 1; $i <= 5; $i++): ?>
							<option value="<?= $i; ?>"<?php echo ($isSaveData && $questionData["point"] == $i) ? " selected" : ""; ?>><?= $i; ?></option>
						<?php endfor; ?>
					</select>
				</dd>
			</dl>
		</div>
	</div>

	<div class="emegra card" style="margin-top: 32px;">
		<div class="card-header">情報</div>

		<div class="card-body">
			<style type="text/css">
				dl.emegra.info { grid-template-columns: 140px 1fr; }
			</style>
			<dl class="emegra info">
				<dt class="essential">作成者</dt>
				<dd>
					<select class="form-select" name="admin_regist" style="width: 150px;">
						<option value="" hidden>選択</option>
						<?php foreach($adminDatas as $data): ?>
							<option value="<?php echo $data["code"]; ?>"<?php echo $data["code"] == $_SESSION["user"]["code"] ? " selected" : ""; ?>><?php echo $data["name"]; ?></option>
						<?php endforeach; ?>
					</select>
				</dd>

				<dt class="essential">作成日</dt>
				<dd><input class="form-control" name="date_regist" id="date_regist" type="date" style="width: 150px;" disabled value="<?php echo ($isSaveData) ? date("Y-m-d", strtotime($questionData["date_regist"])) : date("Y-m-d"); ?>"></dd>
			
				<dt>備考</dt>
				<dd class="expand">
					<textarea class="form-control" name="note" rows="5"><?php echo ($isSaveData) ? $questionData["note"] : ""; ?></textarea>
				</dd>
			</dl>
		</div>
	</div>
</form>

<div class="btn-place" style="margin-top: 32px;">
	<button class="emegra cancel" onclick="history.back();">戻る</button>
	<?php if ((isset($mode) && ($mode == "edit"))): ?>
		<button class="emegra" onclick="Update();">修正</button>
	<?php else: ?>
		<button class="emegra" onclick="Regist();">登録</button>
	<?php endif; ?>
</div>

<script type="text/javascript">
	const numTexts = ["①", "②", "③", "④", "⑤"];

	const type = document.getElementById("type");
	const answers = document.getElementById("answers");
	const correct = document.getElementById("correct");
	const dateRegist = document.getElementById("date_regist");

	const form = document.getElementById("form");

	let answerCount = <?= $answerCount; ?>;
	let datetime = "";

	window.onload = function() {
		const now = new Date();
		datetime = now.toISOString().replace("T", " ").substring(0,19);
	}

	function ChangeType(value) {
		answerCount = Number(value) + 3;

		answerChildren = Array.from(answers.children);
		answerChildren.forEach(element => { element.remove(); });
		
		correctChildren = Array.from(correct.children);
		correctChildren.forEach(element => { element.remove(); });
		
		for (let i = 0; i < Number(value) + 3; i++) {
			let dt = document.createElement("dt");
			dt.innerText = numTexts[i];
			answers.appendChild(dt);

			let dd = document.createElement("dd");
			let input = document.createElement("input");
			input.classList.add("form-control");
			input.name = "answer" + i;
			input.type = "text";
			dd.appendChild(input);
			answers.appendChild(dd);

			let option = document.createElement("option");
			option.value = i;
			option.innerText = numTexts[i];
			correct.appendChild(option);
		}
	}

	<?php if (!(isset($mode) && ($mode == "edit"))): ?>
		function Regist() {
			if (form.class1st.value == "") {
				RequestSelect(form.class1st, "大分類");
				return false;
			}

			if (form.class2nd.value == "") {
				RequestSelect(form.class2nd, "小分類");
				return false;
			}

			if (form.level.value == "") {
				RequestSelect(form.level, "レベル");
				return false;
			}

			if (form.type.value == "") {
				RequestSelect(form.type, "問題タイプ");
				return false;
			}

			if (form.title.value.trim() == "") {
				alert("タイトルを入力してください");
				setTimeout("form.title.focus()", 100);
				return false;
			}

			if (form.question.value.trim() == "") {
				alert("問題を入力してください");
				setTimeout("form.question.focus()", 100);
				return false;
			}
			
			for (let i = 0; i < answerCount; i++) {
				var element = document.getElementsByName("answer" + i)[0];

				if (element.value.trim() == "") {
					alert("答え" + (Number(i) + 1) + "を入力してください");
					setTimeout("form.answer" + i + ".focus()", 100);
					return false;
				}
			}

			if (form.correct.value == "") {
				RequestSelect(form.correct, "正解番号");
				return false;
			}

			if (form.point.value == "") {
				RequestSelect(form.point, "点数");
				return false;
			}

			dateRegist.disabled = false;
			dateRegist.type = "text";
			dateRegist.value = datetime;
			
			form.submit();
		}
	<?php endif; ?>

function Update() {
	const postForm = new FormData();

	const xhr = new XMLHttpRequest();
	xhr.open("POST", "../save", true);
	
	postForm.append("class1st", form.class1st.value);
	postForm.append("class2nd", form.class2nd.value);
	postForm.append("level", form.level.value);
	postForm.append("type", form.type.value);
	postForm.append("title", form.title.value);
	postForm.append("question", form.question.value);
	
	let answerList = document.querySelectorAll("input[name*='answer']");
	let answers = "";
	answerList.forEach(element => { answers += element.value + ","; });
	postForm.append("answers", answers.slice(0, -1));

	postForm.append("level", form.level.value);
	postForm.append("correct", form.correct.value);
	postForm.append("point", form.point.value);
	postForm.append("admin_regist", form.admin_regist.value);
	postForm.append("date_regist", dateRegist);
	postForm.append("admin_regist", form.admin_regist.value);
	postForm.append("note", form.note.value);

	<?php if ($isSaveData): ?>
		postForm.append("idx", <?php echo $questionData["idx"]; ?>);
	<?php endif; ?>
	xhr.send(postForm);
	
	xhr.onload = () => {
		if (xhr.status == 200) {
			const result = xhr.responseText;
			if (result == "success") {
				alert("修正しました。");
				self.location.href = "../detail/<?php echo isset($questionData["idx"]) ? $questionData["idx"] : ""; ?>";
			}
			else { alert("Error"); }
		}
	}
}

	function RequestSelect(element, text) {
		alert(text + "を選択してください");
		element.focus();
	}
</script>