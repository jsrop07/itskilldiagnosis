<?php
	if (!isset($_SESSION)) { session_start(); }
	$numTexts = array("①", "②", "③", "④", "⑤");
?>

<?php echo $this->partial("breadcrumb", ["datas" => $breadcrumbData]); ?>

<h1 class="title"><?php echo $title; ?></h1>

<div class="emegra card">
	<div class="card-header">設定</div>

	<div class="card-body">
		<style type="text/css">
			dl.emegra.setting { grid-template-columns: repeat(2, 90px 1fr); }
		</style>
		<dl class="emegra setting">
			<div style="grid-column: 1 / 3; display: grid; grid-template-columns: repeat(2, 90px 1fr); grid-gap: 8px;">
				<dt>大分類</dt>
				<dd>
					<select class="form-select" style="width: 150px;">
						<?php foreach($classLargeDatas as $data): ?>
							<option value="<?php echo $data["text"]; ?>"><?php echo $data["text"]; ?></option>
						<?php endforeach; ?>
					</select>
				</dd>

				<dt>小分類</dt>
				<dd>
					<select class="form-select" style="width: 150px;">
						<?php foreach($classSmallDatas as $data): ?>
							<option value="<?php echo $data["text"]; ?>"><?php echo $data["text"]; ?></option>
						<?php endforeach; ?>
					</select>
				</dd>
			</div>
		
			<dt>レベル</dt>
			<dd>
				<select class="form-select" style="width: 100px;">
					<option value="初級">初級</option>
					<option value="中級">中級</option>
					<option value="高級">高級</option>
				</select>
			</dd>

			<dt>問題タイプ</dt>
			<dd>
				<select class="form-select" style="width: 150px;">
					<option value="3項選択">3項選択</option>
					<option value="4項選択">4項選択</option>
					<option value="5項選択">5項選択</option> 
				</select>
			</dd>

			<dt>登録日</dt>
			<dd><input type="date" class="form-control" style="width: 150px;" disabled value="<?php echo date("Y-m-d"); ?>"></dd>
		
			<dt>タイトル</dt>
			<dd style="grid-column: 2 / 5;"><input type="text" class="form-control"></dd>
		</dl>
	</div>
</div>

<div class="emegra card" style="margin-top: 32px;">
	<div class="card-header">問題</div>

	<div class="card-body">
		<style type="text/css">
			dl.emegra.question { grid-template-columns: 90px 1fr; }
		</style>
		<dl class="emegra question">
			<dt>問題</dt>
			<dd><input type="text" class="form-control"></dd>

			<dt>答え</dt>
			<div style="display: grid; grid-template-columns: 15px 1fr; grid-gap: 24px 8px;">
				<?php foreach($numTexts as $text): ?>
					<dt><?php echo $text; ?></dt>
					<dd><input type="text" class="form-control"/></dd>
				<?php endforeach; ?>
			</div>

			<dt>正解番号</dt>
			<dd>
				<select class="form-select" style="width: 70px;">
					<?php foreach($numTexts as $idx => $text): ?>
						<option value="<?php echo $idx; ?>"><?php echo $text; ?></option>
					<?php endforeach; ?>
				</select>
			</dd>

			<dt>点数</dt>
			<dd>
				<select class="form-select" style="width: 70px;">
					<option value="1">1</option>
					<option value="2">2</option>
					<option value="3">3</option>
					<option value="4">4</option>
					<option value="5">5</option>
				</select>
			</dd>
		</dl>
	</div>
</div>

<div class="emegra card" style="margin-top: 32px;">
	<div class="card-header">情報</div>

	<div class="card-body">
		<style type="text/css">
			dl.emegra.info { grid-template-columns: repeat(2, 90px 1fr); }
		</style>
		<dl class="emegra info">
			<dt>作成者</dt>
			<dd>
				<select class="form-select" style="width: 150px;">
					<?php foreach($adminDatas as $data): ?>
						<option value="<?php echo $data["code"]; ?>"<?php echo $data["code"] == $_SESSION["user"]["code"] ? " selected" : ""; ?>><?php echo $data["name"]; ?></option>
					<?php endforeach; ?>
				</select>
			</dd>

			<dt>作成日</dt>
			<dd><input type="date" class="form-control" style="width: 150px;" disabled value="<?php echo date("Y-m-d"); ?>"></dd>

			<dt>承認者</dt>
			<dd>
				<select class="form-select" style="width: 150px;">
					<?php foreach($adminDatas as $data): ?>
						<option><?php echo $data["name"]; ?></option>
					<?php endforeach; ?>
				</select>
			</dd>
			
			<dt>備考</dt>
			<dd class="expand" style="grid-column: 2 / 5;">
				<textarea class="form-control" rows="5"></textarea>
			</dd>
		</dl>
	</div>
</div>

<div class="btn-place" style="margin-top: 32px;">
	<button class="emegra cancel" onclick="history.back();">戻る</button>
</div>